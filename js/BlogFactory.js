ngio.factory('Blog', ['$firebase', '$q',
    function ($firebase, $q) {
        var getBlogEntry = function getBlogEntry(id) {
            
        };
        
        return {
            getBlogEntry: getBlogEntry
            
        };
    }
]);